// Empty stub for Next.js boundary guards (server-only / client-only).
// These packages only throw when imported on the wrong side of the RSC
// boundary; in the Vitest node environment they have no behavior to test,
// so we alias them to this no-op module.
export {};
